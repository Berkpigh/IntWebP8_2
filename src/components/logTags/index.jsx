function logTags({ tg }) {
  const tArr = { tg }
  const tLen = tArr.length
  console.log(tLen)
  return  (<div>
    for (let i = 0; i < {tLen}; i++) {{tArr[i] }}
    </div>
  )
}
export default logTags
